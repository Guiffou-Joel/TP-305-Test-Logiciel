Pour executer les test il faut lancer la commande suivante dans un terminal avec pour repertoire courant le repertoire parent du code:

python3 testfacturation.py -v

NB: ce programme s'execute sur des systemes d'exploitation GNU/LINUX
